# Use this file to create a conda environment using:
# conda create -n metabolabpy python=3.7 --file conda-requirements.txt -c intel -c conda-forge -c bioconda
#
# to install accelerated packages for intel compatible processors run:
#
#	conda install numpy scipy scikit-learn -c intel --no-update-deps -y

numpy=1.16.4
scipy==1.3.0
matplotlib==3.0.3
pyside2
